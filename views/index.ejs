
        <!-- Head -->
        <%- include('./partials/head') %> 
    <title>LibreArte</title>
</head>

<body>

    <!-- Header -->
    <%- include('./partials/header') %> 
    

    <main>
        <section class="landing">
            <img src="/images/landing.webp" alt="promotional image">
            <img class="landing2" src="/images/landing2.jpg" alt="promotional image">
            <div>
                <h2 class="textOfertas">Aprovechá los descuentos por la vuelta a clases!</p>
            </div>
        </section>

        <section class="destacados">
            <h2 id="destacados">Destacados</h2>
                <% for (let i=0; i < productos.length; i++){ %>
                    <% if(productos[i].products_categories_id == 1) {%>
                        <article class="destacados">
                            <div>
                                <h4>
                                    <%= productos[i].title %>
                                </h4>
                                <a href="/products/<%= productos[i].id %> "><img
                                    src="./images/products/<%= productos[i].img %>"></a>
                            </div>
                        </article>
                        <% }%> 

                    <% }%>
        </section>
    </main>

    <!-- Footer -->
    <%- include('./partials/footer') %> 